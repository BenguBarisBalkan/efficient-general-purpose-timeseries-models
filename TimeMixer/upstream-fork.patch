diff --git a/data_provider/data_factory.py b/data_provider/data_factory.py
index 205fab5..f853ffc 100755
--- a/data_provider/data_factory.py
+++ b/data_provider/data_factory.py
@@ -74,6 +74,11 @@ def data_provider(args, flag):
     else:
         if args.data == 'm4':
             drop_last = False
+        # Few-shot: only forwarded when actually requested, so loaders that don't accept
+        # `percent` (M4/PEMS/Solar) are untouched on the normal full-data path.
+        extra = {}
+        if getattr(args, 'percent', 100) < 100:
+            extra['percent'] = args.percent
         data_set = Data(
             root_path=args.root_path,
             data_path=args.data_path,
@@ -83,7 +88,8 @@ def data_provider(args, flag):
             target=args.target,
             timeenc=timeenc,
             freq=freq,
-            seasonal_patterns=args.seasonal_patterns
+            seasonal_patterns=args.seasonal_patterns,
+            **extra
         )
         print(flag, len(data_set))
         data_loader = DataLoader(
diff --git a/data_provider/data_loader.py b/data_provider/data_loader.py
index 9d03c39..5ae18fe 100755
--- a/data_provider/data_loader.py
+++ b/data_provider/data_loader.py
@@ -19,7 +19,7 @@ warnings.filterwarnings('ignore')
 class Dataset_ETT_hour(Dataset):
     def __init__(self, root_path, flag='train', size=None,
                  features='S', data_path='ETTh1.csv',
-                 target='OT', scale=True, timeenc=0, freq='h', seasonal_patterns=None):
+                 target='OT', scale=True, timeenc=0, freq='h', seasonal_patterns=None, percent=100):
         # size [seq_len, label_len, pred_len]
         # info
         if size == None:
@@ -43,6 +43,7 @@ class Dataset_ETT_hour(Dataset):
 
         self.root_path = root_path
         self.data_path = data_path
+        self.percent = percent  # few-shot: %% of the TRAIN split to keep
         self.__read_data__()
 
     def __read_data__(self):
@@ -55,6 +56,11 @@ class Dataset_ETT_hour(Dataset):
         border1 = border1s[self.set_type]
         border2 = border2s[self.set_type]
 
+        # Few-shot setting: keep only the first `percent`%% of the training split. Applies to the
+        # train split only; the scaler below still fits the full train range (matches TSLib).
+        if self.set_type == 0 and getattr(self, 'percent', 100) < 100:
+            border2 = (border2 - self.seq_len) * self.percent // 100 + self.seq_len
+
         if self.features == 'M' or self.features == 'MS':
             cols_data = df_raw.columns[1:]
             df_data = df_raw[cols_data]
@@ -107,7 +113,7 @@ class Dataset_ETT_hour(Dataset):
 class Dataset_ETT_minute(Dataset):
     def __init__(self, root_path, flag='train', size=None,
                  features='S', data_path='ETTm1.csv',
-                 target='OT', scale=True, timeenc=0, freq='t', seasonal_patterns=None):
+                 target='OT', scale=True, timeenc=0, freq='t', seasonal_patterns=None, percent=100):
         # size [seq_len, label_len, pred_len]
         # info
         if size == None:
@@ -131,6 +137,7 @@ class Dataset_ETT_minute(Dataset):
 
         self.root_path = root_path
         self.data_path = data_path
+        self.percent = percent  # few-shot: %% of the TRAIN split to keep
         self.__read_data__()
 
     def __read_data__(self):
@@ -143,6 +150,11 @@ class Dataset_ETT_minute(Dataset):
         border1 = border1s[self.set_type]
         border2 = border2s[self.set_type]
 
+        # Few-shot setting: keep only the first `percent`%% of the training split. Applies to the
+        # train split only; the scaler below still fits the full train range (matches TSLib).
+        if self.set_type == 0 and getattr(self, 'percent', 100) < 100:
+            border2 = (border2 - self.seq_len) * self.percent // 100 + self.seq_len
+
         if self.features == 'M' or self.features == 'MS':
             cols_data = df_raw.columns[1:]
             df_data = df_raw[cols_data]
@@ -197,7 +209,7 @@ class Dataset_ETT_minute(Dataset):
 class Dataset_Custom(Dataset):
     def __init__(self, root_path, flag='train', size=None,
                  features='S', data_path='ETTh1.csv',
-                 target='OT', scale=True, timeenc=0, freq='h', seasonal_patterns=None):
+                 target='OT', scale=True, timeenc=0, freq='h', seasonal_patterns=None, percent=100):
         # size [seq_len, label_len, pred_len]
         # info
         if size == None:
@@ -221,6 +233,7 @@ class Dataset_Custom(Dataset):
 
         self.root_path = root_path
         self.data_path = data_path
+        self.percent = percent  # few-shot: %% of the TRAIN split to keep
         self.__read_data__()
 
     def __read_data__(self):
@@ -243,6 +256,11 @@ class Dataset_Custom(Dataset):
         border1 = border1s[self.set_type]
         border2 = border2s[self.set_type]
 
+        # Few-shot setting: keep only the first `percent`%% of the training split. Applies to the
+        # train split only; the scaler below still fits the full train range (matches TSLib).
+        if self.set_type == 0 and getattr(self, 'percent', 100) < 100:
+            border2 = (border2 - self.seq_len) * self.percent // 100 + self.seq_len
+
         if self.features == 'M' or self.features == 'MS':
             cols_data = df_raw.columns[1:]
             df_data = df_raw[cols_data]
@@ -841,6 +859,11 @@ class Dataset_Solar(Dataset):
         border1 = border1s[self.set_type]
         border2 = border2s[self.set_type]
 
+        # Few-shot setting: keep only the first `percent`%% of the training split. Applies to the
+        # train split only; the scaler below still fits the full train range (matches TSLib).
+        if self.set_type == 0 and getattr(self, 'percent', 100) < 100:
+            border2 = (border2 - self.seq_len) * self.percent // 100 + self.seq_len
+
         df_data = df_raw.values
 
         if self.scale:
diff --git a/exp/exp_anomaly_detection.py b/exp/exp_anomaly_detection.py
index f4dd4e2..38cedc5 100644
--- a/exp/exp_anomaly_detection.py
+++ b/exp/exp_anomaly_detection.py
@@ -211,7 +211,8 @@ class Exp_Anomaly_Detection(Exp_Basic):
             accuracy, precision,
             recall, f_score))
 
-        f = open("result_anomaly_detection.txt", 'a')
+        os.makedirs("./logs", exist_ok=True)
+        f = open("./logs/result_anomaly_detection.txt", "a")
         f.write(setting + "  \n")
         f.write("Accuracy : {:0.4f}, Precision : {:0.4f}, Recall : {:0.4f}, F-score : {:0.4f} ".format(
             accuracy, precision,
diff --git a/exp/exp_basic.py b/exp/exp_basic.py
index 6d093c4..cb157d9 100755
--- a/exp/exp_basic.py
+++ b/exp/exp_basic.py
@@ -1,6 +1,6 @@
 import os
 import torch
-from models import TimeMixer
+from models import TimeMixer, TimeMixerPP, SparseTSF
 
 
 class Exp_Basic(object):
@@ -8,6 +8,8 @@ class Exp_Basic(object):
         self.args = args
         self.model_dict = {
             'TimeMixer': TimeMixer,
+            'TimeMixerPP': TimeMixerPP,
+            'SparseTSF': SparseTSF,
         }
         self.device = self._acquire_device()
         self.model = self._build_model().to(self.device)
diff --git a/exp/exp_classification.py b/exp/exp_classification.py
index 499d28c..e295de0 100644
--- a/exp/exp_classification.py
+++ b/exp/exp_classification.py
@@ -38,8 +38,13 @@ class Exp_Classification(Exp_Basic):
         return data_set, data_loader
 
     def _select_optimizer(self):
-        # model_optim = optim.Adam(self.model.parameters(), lr=self.args.learning_rate)
-        model_optim = optim.RAdam(self.model.parameters(), lr=self.args.learning_rate)
+        # RAdam was only added to torch.optim in newer releases; this repo's pinned
+        # torch==1.7.1 does not have it. Fall back to Adam so classification runs on the
+        # local env, while still preferring RAdam when available (matches upstream).
+        if hasattr(optim, 'RAdam'):
+            model_optim = optim.RAdam(self.model.parameters(), lr=self.args.learning_rate)
+        else:
+            model_optim = optim.Adam(self.model.parameters(), lr=self.args.learning_rate)
         return model_optim
 
     def _select_criterion(self):
diff --git a/exp/exp_imputation.py b/exp/exp_imputation.py
index 0014fa4..a67e7bd 100644
--- a/exp/exp_imputation.py
+++ b/exp/exp_imputation.py
@@ -241,7 +241,8 @@ class Exp_Imputation(Exp_Basic):
 
         mae, mse, rmse, mape, mspe = metric(preds[masks == 0], trues[masks == 0])
         print('mse:{}, mae:{}'.format(mse, mae))
-        f = open("result_imputation.txt", 'a')
+        os.makedirs("./logs", exist_ok=True)
+        f = open("./logs/result_imputation.txt", "a")
         f.write(setting + "  \n")
         f.write('mse:{}, mae:{}'.format(mse, mae))
         f.write('\n')
diff --git a/exp/exp_long_term_forecasting.py b/exp/exp_long_term_forecasting.py
index 33274be..42de431 100755
--- a/exp/exp_long_term_forecasting.py
+++ b/exp/exp_long_term_forecasting.py
@@ -181,12 +181,31 @@ class Exp_Long_Term_Forecast(Exp_Basic):
                     iter_count = 0
                     time_now = time.time()
 
+                # Skip non-finite loss batches: a single NaN/inf batch otherwise
+                # backprops into the weights and poisons the whole run. Dropping it
+                # keeps weights clean and lets training continue.
+                if not torch.isfinite(loss):
+                    if len(train_loss) > 0:
+                        train_loss.pop()  # remove the NaN we just appended
+                    model_optim.zero_grad()
+                    continue
+
                 if self.args.use_amp:
                     scaler.scale(loss).backward()
                     scaler.step(model_optim)
                     scaler.update()
                 else:
                     loss.backward()
+                    # clip_grad_norm_ returns the total grad norm. If it is non-finite,
+                    # the gradients contain NaN/inf (which output-sanitization can hide
+                    # from the loss); stepping the optimiser would poison all weights.
+                    # Skip the step in that case so weights stay clean.
+                    total_norm = nn.utils.clip_grad_norm_(self.model.parameters(), max_norm=1.0)
+                    if not torch.isfinite(total_norm):
+                        model_optim.zero_grad()
+                        if len(train_loss) > 0:
+                            train_loss.pop()
+                        continue
                     model_optim.step()
 
                 if self.args.lradj == 'TST':
@@ -280,8 +299,22 @@ class Exp_Long_Term_Forecast(Exp_Basic):
                     pd = np.concatenate((input[0, :, -1], pred[0, :, -1]), axis=0)
                     visual(gt, pd, os.path.join(folder_path, str(i) + '.pdf'))
 
-        preds = np.concatenate(preds, axis=0)
-        trues = np.concatenate(trues, axis=0)
+        # np.concatenate needs the batch list AND the output alive at once (2x peak memory), which
+        # MemoryErrors on wide datasets with long horizons (e.g. Electricity: 4864 x 720 x 321
+        # float32 ~ 4.2 GB per array). Copy batch-by-batch and drop each batch as it is consumed:
+        # numerically identical, roughly half the peak memory.
+        def _stack(chunks):
+            n = sum(c.shape[0] for c in chunks)
+            out = np.empty((n,) + chunks[0].shape[1:], dtype=chunks[0].dtype)
+            j = 0
+            while chunks:
+                c = chunks.pop(0)
+                out[j:j + c.shape[0]] = c
+                j += c.shape[0]
+            return out
+
+        preds = _stack(preds)
+        trues = _stack(trues)
         preds = preds.reshape(-1, preds.shape[-2], preds.shape[-1])
         trues = trues.reshape(-1, trues.shape[-2], trues.shape[-1])
 
@@ -292,4 +325,9 @@ class Exp_Long_Term_Forecast(Exp_Basic):
 
         mae, mse, rmse, mape, mspe = metric(preds, trues)
         print('mse:{}, mae:{}'.format(mse, mae))
+        # Extra metrics, needed for PEMS-style reporting (MAE/MAPE/RMSE). The UPPERCASE labels are
+        # deliberate: the benchmark runners regex lowercase `mse:` / `mae:` and take the LAST match,
+        # so a lowercase "rmse:" would match the `mse:` pattern and silently corrupt every existing
+        # results table. Uppercase keeps this line invisible to those parsers.
+        print('RMSE:{}, MAPE:{}, MSPE:{}'.format(rmse, mape, mspe))
         return
diff --git a/requirements.txt b/requirements.txt
index 4b67c44..0bab4f5 100755
--- a/requirements.txt
+++ b/requirements.txt
@@ -1,7 +1,7 @@
 einops==0.4.1
 matplotlib==3.4.3
 numpy==1.22.4
-pandas==1.1.5
+pandas==1.3.5
 patool==1.12
 reformer_pytorch==1.4.4
 scikit_learn==1.2.1
@@ -10,6 +10,9 @@ sktime==0.4.1
 sympy==1.11.1
 torch==1.7.1
 tqdm==4.64.0
-# only test latest version of sktime
-sktime==0.29.1; python_version == "3.8"
-PyWavelets==1.9.0
+sktime
+PyWavelets
+# Energy/carbon tracking (opt-in via run.py --track_emissions).
+# importlib_resources is a Python<3.9 backport that codecarbon 3.x needs but does not declare.
+codecarbon==3.2.3
+importlib_resources; python_version < "3.9"
diff --git a/run.py b/run.py
index acf120c..e03c184 100755
--- a/run.py
+++ b/run.py
@@ -107,6 +107,24 @@ parser.add_argument('--p_hidden_dims', type=int, nargs='+', default=[128, 128],
                     help='hidden layer dimensions of projector (List)')
 parser.add_argument('--p_hidden_layers', type=int, default=2, help='number of hidden layers in projector')
 
+# SparseTSF params (only read by --model SparseTSF; additive, other models ignore them)
+parser.add_argument('--period_len', type=int, default=24, help='SparseTSF period length (must divide seq_len and pred_len)')
+parser.add_argument('--model_type', type=str, default='linear', help='SparseTSF variant: linear (<1k params) or mlp')
+
+# few-shot forecasting: keep only this %% of the TRAIN split (100 = normal full-data training).
+# Only read by the ETT/Custom loaders, and only passed through when < 100, so default runs are
+# byte-for-byte unaffected.
+parser.add_argument('--percent', type=int, default=100,
+                    help='percent of training data to use (few-shot); 100 = all')
+
+# energy / carbon tracking (opt-in; off by default so normal runs are unaffected)
+parser.add_argument('--track_emissions', type=int, default=0,
+                    help='1 to measure energy/CO2 of train+test with codecarbon')
+parser.add_argument('--emissions_output_dir', type=str, default='.',
+                    help='directory for the codecarbon output csv')
+parser.add_argument('--emissions_file', type=str, default='emissions.csv',
+                    help='codecarbon output csv filename')
+
 if __name__ == '__main__':
     args = parser.parse_args()
     args.use_gpu = True if torch.cuda.is_available() and args.use_gpu else False
@@ -155,11 +173,33 @@ if __name__ == '__main__':
                 args.des, ii)
 
             exp = Exp(args)  # set experiments
-            print('>>>>>>>start training : {}>>>>>>>>>>>>>>>>>>>>>>>>>>'.format(setting))
-            exp.train(setting)
 
-            print('>>>>>>>testing : {}<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<'.format(setting))
-            exp.test(setting)
+            # Optional energy/CO2 measurement around train+test. codecarbon is imported
+            # lazily so it is only a dependency when --track_emissions is actually used.
+            tracker = None
+            if args.track_emissions:
+                from codecarbon import EmissionsTracker
+                tracker = EmissionsTracker(
+                    project_name=setting,
+                    output_dir=args.emissions_output_dir,
+                    output_file=args.emissions_file,
+                    save_to_file=True,
+                    log_level='error',
+                )
+                tracker.start()
+
+            try:
+                print('>>>>>>>start training : {}>>>>>>>>>>>>>>>>>>>>>>>>>>'.format(setting))
+                exp.train(setting)
+
+                print('>>>>>>>testing : {}<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<'.format(setting))
+                exp.test(setting)
+            finally:
+                if tracker is not None:
+                    emissions_kg = tracker.stop()
+                    # printed so the runner can parse it straight from the log
+                    print('emissions_kg:{}'.format(emissions_kg))
+
             torch.cuda.empty_cache()
     else:
         ii = 0
diff --git a/utils/tools.py b/utils/tools.py
index caf9651..6ca417e 100755
--- a/utils/tools.py
+++ b/utils/tools.py
@@ -39,6 +39,15 @@ class EarlyStopping:
         self.delta = delta
 
     def __call__(self, val_loss, model, path):
+        # A NaN/inf validation loss must never count as an improvement, otherwise
+        # it overwrites a good checkpoint with diverged weights and resets the
+        # early-stop counter. Treat it as a non-improving epoch.
+        if not np.isfinite(val_loss):
+            self.counter += 1
+            print(f'EarlyStopping counter: {self.counter} out of {self.patience} (non-finite val_loss, checkpoint preserved)')
+            if self.counter >= self.patience:
+                self.early_stop = True
+            return
         score = -val_loss
         if self.best_score is None:
             self.best_score = score
